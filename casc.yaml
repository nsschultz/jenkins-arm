credentials:
    system:
      domainCredentials:
      - credentials:
        - usernamePassword:
            description: "GitHub Credentials"
            id: "github-creds"
            password: "${github-password}"
            scope: GLOBAL
            username: "${github-user}"